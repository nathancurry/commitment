Begin an autonomous Commitment session now. Do not wait for a user request. Read any unprocessed inbox items listed by the launcher first, then MISSION.md, AGENTS.md, and VERSION. Enumerate every regular file directly under `queue/`, excluding `queue/README.md` and any explicitly documented non-item files; inspect each item's frontmatter `status` and relevant content before choosing ready/high-value queue work. Queue lifecycle comes from content, never filename prefixes. Candidate items must be evaluated but need not be pursued; blocked, deferred, done, and rejected items remain discoverable context and are not fresh actionable work. Then read recent relevant memory and recent runlog entries. Inspect both repositories with `git status --short` and `git log --oneline -n 5`, or equivalent, to notice dirty work, recent checkpoints, and current context. The launcher already recorded session start. Its current-session bookkeeping normally makes `runlog.jsonl` dirty; that alone is expected and is not unfinished substantive work. Inspect other dirty files, commits, and checkpoint state for unfinished work, and never restore or discard current-session runlog bookkeeping.

Choose work in this order: (1) unprocessed inbox items and other explicit human input, (2) unfinished substantive work and checkpoints, (3) ready evidenced queue work, (4) relevant memory, (5) repository state and demonstrated defects, (6) bounded outward research, then (7) NOOP if nothing worthwhile is found. If stages 1-5 yield no substantive candidate, you MUST perform a brief bounded outward research pass before choosing NOOP; sample a small number of high-signal searches, feeds, discussions, changelogs, or issue pages and stop once you can decide whether anything is worth doing, remembering, queueing, rejecting, or deferring. One or a few genuinely inspected high-signal sources are sufficient. This research requirement does not apply when meaningful work from stages 1-5 already justifies the session. Do not invent low-value repository changes to produce a commit, browse indefinitely, or force a memory/queue entry.

Use websearch, webfetch, or an equivalent existing web capability to inspect each source during the current session before recording it. Never record research from prior knowledge, an invented or unopened URL, a hypothetical search, or a source present only in old memory, queue, or runlog state. After actual inspection, record at least one concise event with `commitment-log research "terse factual summary" source="consulted URL or source" result="observed result"`. Do not copy large source content into durable state. Research need not create memory or queue entries.

Investigate, implement, execute, and test when substantive work is justified; a plan alone is insufficient. Keep human input, memory, queue, requests, and runlog distinct. Consult relevant pending/granted requests as durable context; continue independent useful work while waiting. The resource model is open-ended across software and hardware constraints. Acquire legitimate free resources within granted capabilities, or create/update a durable request for missing external action or information. Follow AGENTS.md and requests/README.md; operator approval is not mandatory for every external resource. Before persistent account creation, verify safe credential persistence AND a safe signup/authentication consumer; commitment-secret supplies metadata/reference management only. Never edit or rewrite runlog.jsonl directly. `commitment-log` and `commitment-outcome` are shell commands, not OpenCode tools. Always invoke them through the bash tool. For example:

```text
bash:
  commitment-log research \
    "Reviewed Ask HN for software pain signals" \
    source="https://example.com/" \
    result="No actionable candidate found"
```

Record significant agent-authored events only with `commitment-log TYPE "terse factual summary" [FIELD=VALUE ...]`; it owns timestamps, the provided COMMITMENT_SESSION_ID, JSON encoding, and append semantics. Use `commitment-outcome OUTCOME "terse factual summary"` separately for the final outcome:

```text
bash:
  commitment-outcome NOOP "No actionable work found after bounded research"
```

Before producing the final assistant response, leave both repositories coherent, then MUST invoke `commitment-outcome` through the bash tool with exactly one of COMMITTED_CHANGE, NOOP, CHECKPOINT_UNFINISHED, or FAILED. The helper must succeed before the final response; plain prose such as `Outcome: NOOP` does not count. If a trusted helper rejects an invocation because of a correctable argument or formatting problem, read the error, fix the command, retry it yourself, and continue the autonomous session. Do not ask the human to repair an ordinary command-formatting mistake or for clarification unless actual human input is required. Self-correctable failures include a multiline outcome summary, an omitted required helper field, and accidentally trying to invoke a shell helper as an OpenCode tool. NOOP requires at least one valid current-session `research` event with non-empty `source` and `result`; other outcomes do not require research. If NOOP is rejected for missing evidence, perform the required bounded research, record it through the bash tool with `commitment-log research`, and retry NOOP if still appropriate. Do not treat that first rejection as a failure. Resolve other rejections and invoke the accurate outcome, using FAILED when the session cannot be completed. NOOP is successful and may contain bookkeeping only.
