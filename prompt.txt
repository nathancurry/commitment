Begin an autonomous Commitment session now. Do not wait for a user request. Read MISSION.md, AGENTS.md, VERSION, ready/high-value queue items, recent relevant memory, and recent runlog entries. Inspect both repositories with `git status --short` and `git log --oneline -n 5`, or equivalent, to notice dirty work, recent checkpoints, and current context. The launcher already recorded session start.

Choose work in this order: (1) explicit human input, (2) unfinished substantive work and checkpoints, (3) ready evidenced queue work, (4) relevant memory, (5) repository state and demonstrated defects, (6) bounded outward research, then (7) NOOP if nothing worthwhile is found. If stages 1-5 yield no substantive candidate, you MUST perform a brief bounded outward research pass before choosing NOOP; sample a small number of high-signal searches, feeds, discussions, changelogs, or issue pages and stop once you can decide whether anything is worth doing, remembering, queueing, rejecting, or deferring. One or a few genuinely inspected high-signal sources are sufficient. This research requirement does not apply when meaningful work from stages 1-5 already justifies the session. Do not invent low-value repository changes to produce a commit, browse indefinitely, or force a memory/queue entry.

Use websearch, webfetch, or an equivalent existing web capability to inspect each source during the current session before recording it. Never record research from prior knowledge, an invented or unopened URL, a hypothetical search, or a source present only in old memory, queue, or runlog state. After actual inspection, record at least one concise event with `commitment-log research "terse factual summary" source="consulted URL or source" result="observed result"`. Do not copy large source content into durable state. Research need not create memory or queue entries.

Investigate, implement, execute, and test when substantive work is justified; a plan alone is insufficient. Keep human input, memory, queue, and runlog distinct. Never edit or rewrite runlog.jsonl directly. Record significant agent-authored events only with `commitment-log TYPE "terse factual summary" [FIELD=VALUE ...]`; it owns timestamps, the provided COMMITMENT_SESSION_ID, JSON encoding, and append semantics. Use `commitment-outcome OUTCOME "terse factual summary"` separately for the final outcome.

Before producing the final assistant response, leave both repositories coherent, then MUST invoke `commitment-outcome` with exactly one of COMMITTED_CHANGE, NOOP, CHECKPOINT_UNFINISHED, or FAILED. The helper must succeed before the final response; plain prose such as `Outcome: NOOP` does not count. NOOP requires at least one valid current-session `research` event with non-empty `source` and `result`; other outcomes do not require research. If NOOP is rejected for missing evidence, perform the required bounded research, record it with `commitment-log research`, and retry NOOP if still appropriate. Do not treat that first rejection as a failure. Resolve other rejections and invoke the accurate outcome, using FAILED when the session cannot be completed. NOOP is successful and may contain bookkeeping only.
