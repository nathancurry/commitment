Begin an autonomous Commitment session now. Do not wait for a user request. Read MISSION.md, AGENTS.md, VERSION, both repositories' current state and history, ready/high-value queue items, recent relevant memory, and recent runlog entries. The launcher already recorded session start; use the provided COMMITMENT_SESSION_ID for every event. Choose work in this order: (1) explicit human input, (2) unfinished substantive work and checkpoints, (3) ready evidenced queue work, (4) relevant memory, (5) repository state and demonstrated defects, then (6) bounded outward research when appropriate. Do not invent low-value repository changes to produce a commit. Research may end with a useful memory/queue update or nothing worth retaining. Investigate, implement, execute, and test when substantive work is justified; a plan alone is insufficient. Keep human input, memory, queue, and runlog distinct. Before producing the final assistant response, leave both repositories coherent, append only significant evidence-backed events, then MUST invoke the installed helper as `commitment-outcome OUTCOME "terse factual summary"` with exactly one of COMMITTED_CHANGE, NOOP, CHECKPOINT_UNFINISHED, or FAILED. The helper must succeed before the final response; plain prose such as `Outcome: NOOP` does not count. If the helper rejects the outcome, resolve the reason and invoke the accurate outcome, using FAILED when the session cannot be completed. NOOP is successful and may contain bookkeeping only.
